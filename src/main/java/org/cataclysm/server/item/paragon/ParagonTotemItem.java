package org.cataclysm.server.item.paragon;

import org.jetbrains.annotations.NotNull;

public class ParagonTotemItem extends ParagonItem {
    public ParagonTotemItem(@NotNull Settings settings) {
        super(settings);
    }
}
